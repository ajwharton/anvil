# Agent rules (auto-loaded)

**Session entry:** read **`start.md`** — that file is the full thin card for this project.

When the user says `start`, `read start.md`, or opens a fresh session without a specific file task: **read `start.md` once**, then act. Do not preload design + roadmap + governance in one boot.

**Product dual focus:** Anvil is for **individuals** (UI/CLI) and **agentic control** (HTTP/MCP; user brings the brain). Prefer changes that keep both true. Thesis: `docs/product.md`, `docs/agentic-control.md`.

**Git / PRs (Mia-aligned):** never push to `main`; never merge PRs unless the user explicitly says to merge *this* PR. Branch → `gh pr create` → report URL → stop. See `docs/development-process.md`.

Thin harness: `~/.grok/docs/thin-harness.md`.
