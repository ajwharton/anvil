# Scripts

| Script | Purpose |
|--------|---------|
| `pull_base_model.py` | SSH to **forge/hammer** and pull HF bases onto lab NVMe (`/mnt/data/models`) |

Weights stay on lab hosts and out of git. Default model: Qwen2.5-VL-3B. See [docs/models.md](../docs/models.md).

